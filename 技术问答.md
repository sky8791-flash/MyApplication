# 答辩常见技术问题与回答

## 🎯 Android开发相关

### Q1: 为什么选择Android平台？
**A**: Android是全球使用最广泛的移动操作系统，市场份额超过70%。选择Android开发可以：
- 覆盖更广泛的用户群体
- 开发工具（Android Studio）成熟完善
- 社区资源丰富，学习资料充足
- Java语言我比较熟悉

### Q2: Activity的生命周期在项目中如何应用？
**A**: 项目中充分利用了Activity生命周期：
- `onCreate()`: 初始化UI组件、游戏引擎、蓝牙服务
- `onDestroy()`: 释放蓝牙连接资源，避免内存泄漏
- `onPause()`/`onResume()`: 处理游戏暂停/恢复（未来可扩展）

### Q3: 如何处理内存泄漏问题？
**A**: 采取了以下措施：
- BluetoothHelper在onDestroy中调用close()释放连接
- Handler使用弱引用避免持有Activity引用
- 及时取消注册BroadcastReceiver
- 避免在static变量中持有Context

### Q4: 为什么使用自定义View而不是现有组件？
**A**: 棋盘是复杂的图形界面，标准组件无法满足需求：
- 需要绘制网格、棋子、坐标标识
- 需要处理特殊的触摸交互（象棋拖动）
- 需要高性能渲染，自定义View可以优化绘制
- 三种棋盘样式需要灵活切换

---

## 🧠 算法与数据结构

### Q5: AI算法的时间复杂度是多少？
**A**: 
- **评估函数**: O(n²)，需要遍历整个棋盘
- **寻找最优位置**: O(n² × m)，其中m是候选位置数
- **优化**: 通过减少候选位置、提前剪枝来降低实际复杂度
- **异步处理**: AI计算在后台线程，不影响UI响应

### Q6: 围棋的"气"是如何计算的？
**A**: 使用深度优先搜索（DFS）算法：
```java
// 伪代码
calculateLiberty(int r, int c, int color) {
    visited = new boolean[size][size];
    liberty = 0;
    dfs(r, c, color, visited);
    return liberty;
}

dfs(r, c, color, visited) {
    // 访问相同颜色的连通棋子
    // 统计空位数量（气）
}
```
- 从一个棋子开始DFS遍历所有相连同色棋子
- 统计周围的空位数量
- 时间复杂度: O(n²)

### Q7: 象棋规则验证如何实现？
**A**: 为每种棋子实现独立的验证方法：
- **车**: 横向或纵向直线移动，中间无阻挡
- **马**: 日字形移动，检查蹩马腿
- **象**: 田字形移动，不过河，检查塞象眼
- **士**: 斜向一格，不出九宫
- **将**: 一格移动，不出九宫，不能面对面
- **炮**: 直线移动，吃子需隔一子
- **兵**: 过河前只能前进，过河后可左右

每次移动都会调用相应的验证方法，确保符合规则。

### Q8: 如何防止AI计算时界面卡顿？
**A**: 使用Handler和后台线程：
```java
private void makeAIMove() {
    aiHandler.postDelayed(new Runnable() {
        @Override
        public void run() {
            // AI计算在这里进行
            int[] move = ai.findBestMove(...);
            // 结果在UI线程更新
            runOnUiThread(() -> {
                updateBoard(move);
            });
        }
    }, 500);
}
```

---

## 🔌 蓝牙通信

### Q9: 蓝牙通信协议是如何设计的？
**A**: 使用JSON格式的消息协议，包含以下消息类型：

| 消息类型 | 说明 | 字段 |
|---------|------|------|
| challenge | 发起对战邀请 | type, from |
| accept | 接受邀请 | type |
| reject | 拒绝邀请 | type |
| move | 移动数据 | type, r, c, color, gameType |
| undo_request | 悔棋请求 | type |
| undo_ok | 同意悔棋 | type |

优点：
- 可读性强，易于调试
- 可扩展，添加新字段不影响旧版本
- 跨平台兼容

### Q10: 如何保证蓝牙数据同步的可靠性？
**A**: 
1. **消息确认机制**: challenge-accept模式确保双方准备就绪
2. **状态同步**: 每次move都包含完整的位置和颜色信息
3. **回合控制**: turn变量确保轮流下棋
4. **错误处理**: try-catch捕获通信异常，提示用户

### Q11: 蓝牙权限申请如何处理？
**A**: Android 12+需要特殊蓝牙权限：
```java
private final String[] perms = new String[]{
    Manifest.permission.BLUETOOTH_SCAN,
    Manifest.permission.BLUETOOTH_CONNECT,
    Manifest.permission.ACCESS_FINE_LOCATION
};
```
使用ActivityResultLauncher动态申请，用户拒绝时给出提示。

---

## 💾 数据存储

### Q12: 为什么选择SharedPreferences而不是数据库？
**A**: 
**当前方案的优势**：
- 游戏记录数量不大（一般<100条）
- SharedPreferences读写快速
- JSON序列化简单，易于实现
- 无需额外的数据库框架

**数据库的适用场景**：
- 记录数量非常大（>1000条）
- 需要复杂查询（按日期、对手、胜率等）
- 需要关联多个表

未来如果用户反馈需要更强大的历史记录功能，可以迁移到SQLite。

### Q13: 游戏记录的数据结构是什么？
**A**: 
```java
public class GameRecord {
    private String gameType;      // 游戏类型
    private String gameMode;      // 对战模式
    private String opponentName;  // 对手名称
    private String result;        // 胜/负/平
    private int playerMoves;      // 玩家步数
    private int opponentMoves;    // 对手步数
    private long duration;        // 对战时长
    private long timestamp;       // 时间戳
}
```
使用JSON序列化后保存到SharedPreferences的一个字符串数组中。

---

## 🎨 UI/UX设计

### Q14: Material Design 3有哪些特点？
**A**: 
- **动态配色**: 支持从壁纸提取主题色（本项目未启用）
- **大圆角**: CardView使用28dp圆角
- **更好的深色模式**: Theme.Material3.DayNight
- **新组件**: MaterialCardView, MaterialButton等

本项目使用了MaterialCardView来构建卡片式布局，提供良好的层次感和视觉效果。

### Q15: 如何确保UI在不同屏幕尺寸上的适配？
**A**: 
- 使用dp单位代替px，保证密度独立
- LinearLayout的weight属性实现响应式布局
- 棋盘View根据屏幕大小动态计算格子尺寸
- 测试了不同分辨率的模拟器

### Q16: 为什么使用Kawaii风格？
**A**: 
- 目标用户群体广泛，包括年轻用户
- 可爱风格降低游戏门槛，显得亲和友好
- Emoji图标跨文化，易于理解
- 与传统"严肃"棋类应用形成差异化

---

## 🔧 工程实践

### Q17: 为什么删除MainActivity？
**A**: MainActivity是早期代码，存在以下问题：
- **未被使用**: 不在应用流程中，不是launcher
- **功能重复**: GameActivity已实现所有功能且更完善
- **代码质量**: 保留死代码会降低可维护性
- **混淆风险**: 可能误导其他开发者

删除后：
- 减少~455行代码
- 代码结构更清晰
- 符合YAGNI原则（You Aren't Gonna Need It）

### Q18: 代码是如何组织的？
**A**: 
```
com.example.myapplication/
├── MenuActivity.java          # 主菜单
├── GameActivity.java          # 游戏主界面
├── HistoryActivity.java       # 历史记录
├── BoardView.java            # 自定义棋盘View
├── GomokuEngine.java         # 五子棋引擎
├── GoEngine.java             # 围棋引擎
├── XiangqiEngine.java        # 象棋引擎
├── SimpleAI.java             # AI算法
├── BluetoothHelper.java      # 蓝牙封装
├── GameHistoryManager.java   # 记录管理
└── GameRecord.java           # 记录模型
```
每个类职责单一，符合单一职责原则。

### Q19: 如何保证代码质量？
**A**: 
- **命名规范**: 遵循Java命名约定
- **注释**: 关键算法和复杂逻辑添加注释
- **代码审查**: 通过GitHub进行code review
- **重构**: 及时删除死代码，优化结构
- **版本控制**: 使用Git管理，有完整的commit历史

### Q20: 项目开发过程中遇到的最大挑战？
**A**: 
**象棋规则的完整实现**是最大挑战：
- 7种棋子，每种规则不同
- 特殊规则：蹩马腿、塞象眼、将不能面对面
- 吃子和移动的区别

**解决方案**：
1. 仔细研究象棋规则，列出所有约束
2. 为每种棋子写单独的验证方法
3. 通过实际对弈测试，找出bug
4. 参考象棋规则书籍和在线资料

---

## 🚀 扩展与优化

### Q21: 如果要支持在线对战，需要做什么？
**A**: 
1. **服务器端**：
   - 使用Socket.IO或WebSocket建立实时通信
   - 房间管理系统，匹配玩家
   - 用户认证和会话管理
   
2. **客户端**：
   - 替换BluetoothHelper为NetworkHelper
   - 处理网络延迟和断线重连
   - 同步机制，防止作弊
   
3. **基础设施**：
   - 服务器部署（Node.js/Java Spring）
   - 数据库存储用户和对局数据
   - 负载均衡和扩展

### Q22: 如何添加新的游戏？
**A**: 
1. 创建新的Engine类（如CheckersEngine）
2. 在BoardView中添加新的绘制逻辑
3. 在GameActivity中注册新游戏类型
4. 在MenuActivity添加入口卡片
5. 更新AI算法支持新游戏

架构设计支持这种扩展，无需大改。

### Q23: 性能优化有哪些空间？
**A**: 
**已实施的优化**：
- AI计算异步化
- Canvas绘图避免创建对象
- 只在必要时invalidate()

**未来优化方向**：
- 使用SurfaceView提升绘图性能
- AI算法使用Alpha-Beta剪枝
- 缓存已计算的棋局评估
- 使用线程池管理AI计算任务

---

## 📊 测试相关

### Q24: 进行了哪些测试？
**A**: 
1. **单元测试**:
   - GoEngineTest: 围棋引擎功能测试
   - XiangqiEngineTest: 象棋引擎测试
   
2. **功能测试**:
   - 三种游戏的完整流程
   - AI各难度级别
   - 蓝牙连接和数据传输
   - 悔棋功能
   - 历史记录保存和读取
   
3. **兼容性测试**:
   - 不同Android版本（API 26-34）
   - 不同屏幕尺寸
   - 不同分辨率

### Q25: 如何测试蓝牙功能？
**A**: 
- 使用两台真机测试（模拟器不支持蓝牙）
- 测试场景：
  * 设备发现和连接
  * 对战邀请接受/拒绝
  * 完整对局数据同步
  * 断线重连
  * 悔棋协商
- 确保在不同距离、环境下稳定工作

---

## 💭 项目反思

### Q26: 如果重新做这个项目，会有什么改进？
**A**: 
1. **架构层面**：
   - 使用MVVM架构替代当前的MVC
   - 引入Dagger/Hilt进行依赖注入
   - 使用Room数据库替代SharedPreferences
   
2. **功能层面**：
   - 从一开始就规划在线对战
   - 添加教程和新手引导
   - 实现更强大的AI（Monte Carlo Tree Search）
   
3. **开发流程**：
   - 更完善的单元测试覆盖
   - 使用CI/CD自动化构建和测试
   - 更规范的Git commit message

### Q27: 这个项目让你学到了什么？
**A**: 
1. **技术能力**：
   - Android开发全栈技能
   - 自定义View和Canvas绘图
   - 蓝牙API使用
   - 算法设计和优化
   
2. **工程能力**：
   - 代码组织和架构设计
   - 版本控制和协作
   - 调试和问题解决
   - 文档编写
   
3. **产品思维**：
   - 用户体验设计
   - 功能优先级判断
   - 性能与功能的平衡

---

**准备充分，自信应答！** 💪
