# 📁 项目文件用法和作用总结

## 📱 应用概述

这是一个多功能棋类游戏Android应用，包含五子棋、围棋、象棋三种游戏，支持AI对战和蓝牙联机两种模式。

---

## 🎯 Java源文件（11个）

### 1. MenuActivity.java
**路径**: `app/src/main/java/com/example/myapplication/MenuActivity.java`

**作用**: 应用主菜单界面
- 应用启动后的第一个Activity（LAUNCHER）
- 显示三个游戏选择卡片（五子棋、围棋、象棋）
- 提供历史记录入口按钮
- 通过Intent传递游戏类型参数启动GameActivity

**核心功能**:
```java
// 点击五子棋卡片
Intent intent = new Intent(MenuActivity.this, GameActivity.class);
intent.putExtra("GAME_TYPE", "GOMOKU");
startActivity(intent);
```

**何时使用**: 
- 应用启动时自动加载
- 从游戏界面返回时重新显示

**相关文件**: 
- 布局: `activity_menu.xml`
- 跳转到: `GameActivity.java`, `HistoryActivity.java`

---

### 2. GameActivity.java
**路径**: `app/src/main/java/com/example/myapplication/GameActivity.java`

**作用**: 游戏主界面，负责游戏的核心逻辑
- 接收MenuActivity传递的游戏类型
- 初始化对应的游戏引擎（Gomoku/Go/Xiangqi）
- 管理AI对战和蓝牙对战两种模式
- 处理用户操作（落子、悔棋、返回）
- 保存游戏记录

**核心功能**:
```java
// 获取游戏类型
String gameType = getIntent().getStringExtra("GAME_TYPE");

// 初始化引擎
switch (gameType) {
    case "GOMOKU": gomokuEngine = new GomokuEngine(15); break;
    case "GO": goEngine = new GoEngine(19); break;
    case "XIANGQI": xiangqiEngine = new XiangqiEngine(); break;
}

// AI对战
ai = new SimpleAI(difficulty);

// 蓝牙对战
bt = new BluetoothHelper(this, this);
```

**何时使用**:
- 用户从MenuActivity选择游戏类型后启动
- 管理整个游戏流程直到结束或返回

**相关文件**:
- 布局: `activity_game.xml`
- 引擎: `GomokuEngine.java`, `GoEngine.java`, `XiangqiEngine.java`
- AI: `SimpleAI.java`
- 蓝牙: `BluetoothHelper.java`
- 棋盘: `BoardView.java`
- 历史: `GameHistoryManager.java`

---

### 3. HistoryActivity.java
**路径**: `app/src/main/java/com/example/myapplication/HistoryActivity.java`

**作用**: 历史记录界面
- 显示所有游戏对战记录
- 从GameHistoryManager读取数据
- 以列表形式展示游戏类型、对手、结果、时长等信息

**核心功能**:
```java
// 读取历史记录
GameHistoryManager historyManager = new GameHistoryManager(this);
List<GameRecord> records = historyManager.getRecords();

// 显示在ListView中
adapter = new ArrayAdapter<>(this, ...);
listView.setAdapter(adapter);
```

**何时使用**:
- 用户在MenuActivity点击"对战历史"按钮
- 查看过往对战数据

**相关文件**:
- 布局: `activity_history.xml`, `item_history.xml`
- 数据: `GameHistoryManager.java`, `GameRecord.java`

---

### 4. GomokuEngine.java
**路径**: `app/src/main/java/com/example/myapplication/GomokuEngine.java`

**作用**: 五子棋游戏逻辑引擎
- 管理15×15棋盘状态
- 验证落子是否合法
- 检查五子连珠胜负
- 支持悔棋功能
- 提供历史记录

**核心功能**:
```java
// 落子
public boolean place(int r, int c, int color)

// 检查胜负（检查五子连珠）
public boolean checkWin(int r, int c)

// 悔棋
public void undo(int steps)

// 获取棋盘
public int[][] getBoard()
```

**何时使用**:
- GameActivity中用户选择五子棋时创建
- 每次落子时调用place()
- 每次落子后调用checkWin()

**技术细节**:
- 棋盘大小: 15×15
- 胜利条件: 横/竖/斜任意方向五子连珠

---

### 5. GoEngine.java
**路径**: `app/src/main/java/com/example/myapplication/GoEngine.java`

**作用**: 围棋游戏逻辑引擎
- 管理19×19棋盘状态
- 实现围棋规则（提子判定）
- 计算"气"（连通空位数）
- 检查胜负条件

**核心功能**:
```java
// 落子
public boolean place(int r, int c, int color)

// 计算气（使用DFS算法）
private int calculateLiberty(int r, int c, int color)

// 提子（气为0时移除）
private void captureStones()

// 检查胜负
public boolean checkWin(int r, int c)
```

**何时使用**:
- GameActivity中用户选择围棋时创建
- 每次落子时检查提子规则

**技术细节**:
- 棋盘大小: 19×19
- 核心算法: DFS计算连通性和气

---

### 6. XiangqiEngine.java
**路径**: `app/src/main/java/com/example/myapplication/XiangqiEngine.java`

**作用**: 象棋游戏逻辑引擎
- 管理10×9棋盘和棋子
- 实现所有棋子移动规则
- 验证移动合法性
- 检查将军和获胜

**核心功能**:
```java
// 移动棋子
public boolean place(int fromR, int fromC, int toR, int toC, int color)

// 验证移动规则（车、马、炮、象、士、将、兵）
private boolean isValidMove(...)

// 检查将军
private boolean isInCheck(int color)

// 检查获胜
public boolean checkWin(int r, int c)
```

**何时使用**:
- GameActivity中用户选择象棋时创建
- 用户拖动棋子时验证移动

**技术细节**:
- 棋盘大小: 10×9
- 7种棋子，每种有不同移动规则
- 特殊规则: 蹩马腿、塞象眼、将不能面对面

---

### 7. SimpleAI.java
**路径**: `app/src/main/java/com/example/myapplication/SimpleAI.java`

**作用**: 人工智能对手
- 提供三个难度级别（简单、中等、困难）
- 评估棋局，寻找最优落子位置
- 支持所有三种棋类游戏

**核心功能**:
```java
// 难度枚举
enum Difficulty { EASY, MEDIUM, HARD }

// 寻找最佳位置（五子棋、围棋）
public int[] findBestMoveForBoard(int[][] board, int color, int size)

// 寻找最佳移动（象棋）
public int[] findBestXiangqiMove(XiangqiEngine engine, int color)

// 评估位置价值
private int evaluatePosition(...)
```

**何时使用**:
- 用户在GameActivity点击"AI对战"并选择难度
- 玩家落子后AI自动计算并落子

**技术细节**:
- 使用评估函数给每个位置打分
- 考虑连子数量、威胁防守、位置战略权重
- 不同难度通过调整评估深度和权重实现

---

### 8. BoardView.java
**路径**: `app/src/main/java/com/example/myapplication/BoardView.java`

**作用**: 自定义棋盘视图
- 使用Canvas绘制三种不同的棋盘
- 处理触摸事件（点击落子、拖动移动）
- 实时显示棋盘状态
- 支持三种游戏类型切换

**核心功能**:
```java
// 绘制棋盘
@Override
protected void onDraw(Canvas canvas) {
    // 绘制网格线
    // 绘制棋子
    // 绘制象棋棋子文字
}

// 处理触摸
@Override
public boolean onTouchEvent(MotionEvent event) {
    // 计算点击位置
    // 触发回调
}

// 绑定引擎
public void bindGomokuEngine(GomokuEngine engine)
public void bindGoEngine(GoEngine engine)
public void bindXiangqiEngine(XiangqiEngine engine)

// 设置监听器
public void setOnPlaceListener(OnPlaceListener listener)
public void setOnXiangqiMoveListener(OnXiangqiMoveListener listener)
```

**何时使用**:
- GameActivity初始化时创建
- 每次棋局状态改变时调用invalidate()刷新

**技术细节**:
- 使用Canvas API绘制
- 动态计算格子大小适配屏幕
- 支持点击和拖动两种交互方式

---

### 9. BluetoothHelper.java
**路径**: `app/src/main/java/com/example/myapplication/BluetoothHelper.java`

**作用**: 蓝牙通信封装类
- 管理蓝牙连接（客户端和服务端）
- 设备扫描和发现
- 消息收发（JSON格式）
- 连接状态回调

**核心功能**:
```java
// 开始扫描设备
public void startDiscovery()

// 连接到设备（客户端）
public void connectTo(BluetoothDevice device)

// 等待连接（服务端）
public void startServerAccept()

// 发送消息
public void sendLine(String message)

// 关闭连接
public void close()

// 回调接口
interface Listener {
    void onDeviceFound(BluetoothDevice device);
    void onConnected(BluetoothDevice device);
    void onDisconnected(String reason);
    void onMessage(String line);
    void onError(Throwable t);
}
```

**何时使用**:
- GameActivity初始化时创建
- 用户点击"扫描设备"或"等待连接"
- 蓝牙对战时收发消息

**技术细节**:
- 使用BluetoothSocket进行通信
- JSON格式消息协议
- 后台线程处理网络IO

---

### 10. GameHistoryManager.java
**路径**: `app/src/main/java/com/example/myapplication/GameHistoryManager.java`

**作用**: 游戏记录管理器
- 保存游戏记录到SharedPreferences
- 读取历史记录
- JSON序列化/反序列化
- 数据持久化

**核心功能**:
```java
// 添加记录
public void addRecord(GameRecord record)

// 获取所有记录
public List<GameRecord> getRecords()

// 清空记录
public void clearRecords()

// JSON序列化
private String recordToJson(GameRecord record)

// JSON反序列化
private GameRecord jsonToRecord(String json)
```

**何时使用**:
- GameActivity游戏结束时保存记录
- HistoryActivity读取并显示记录

**技术细节**:
- 使用SharedPreferences存储
- JSON格式保存数据
- 轻量级，适合少量数据

---

### 11. GameRecord.java
**路径**: `app/src/main/java/com/example/myapplication/GameRecord.java`

**作用**: 游戏记录数据模型
- 封装单局游戏的所有信息
- 提供数据访问接口
- 格式化显示方法

**核心功能**:
```java
// 数据字段
private String gameType;      // 游戏类型（五子棋/围棋/象棋）
private String gameMode;      // 对战模式（AI/蓝牙）
private String opponentName;  // 对手名称
private String result;        // 结果（胜/负/平）
private int playerMoves;      // 玩家步数
private int opponentMoves;    // 对手步数
private long duration;        // 对战时长（毫秒）
private long timestamp;       // 时间戳

// 方法
public String getFormattedDuration()  // 格式化时长
public String getFormattedTimestamp() // 格式化时间
```

**何时使用**:
- GameActivity结束时创建并保存
- HistoryActivity读取并显示

**技术细节**:
- POJO（Plain Old Java Object）
- 可序列化为JSON

---

## 🎨 布局文件（4个）

### 1. activity_menu.xml
**路径**: `app/src/main/res/layout/activity_menu.xml`

**作用**: MenuActivity的布局文件
- 主菜单界面UI定义
- 三个游戏选择卡片（Material Design）
- 对战历史按钮

**关键组件**:
```xml
<!-- 五子棋卡片 -->
<MaterialCardView android:id="@+id/cardGomoku">
    <!-- 图标：🎯 -->
    <!-- 标题：五子棋 -->
</MaterialCardView>

<!-- 围棋卡片 -->
<MaterialCardView android:id="@+id/cardGo">
    <!-- 图标：⚫ -->
    <!-- 标题：围棋 -->
</MaterialCardView>

<!-- 象棋卡片 -->
<MaterialCardView android:id="@+id/cardXiangqi">
    <!-- 图标：♟️ -->
    <!-- 标题：象棋 -->
</MaterialCardView>

<!-- 历史按钮 -->
<Button android:id="@+id/btnHistory" />
```

**何时使用**: MenuActivity.setContentView()加载

---

### 2. activity_game.xml
**路径**: `app/src/main/res/layout/activity_game.xml`

**作用**: GameActivity的布局文件
- 游戏主界面UI定义
- 棋盘显示区域
- 功能按钮（AI、蓝牙、悔棋、返回）
- 蓝牙设备列表

**关键组件**:
```xml
<!-- 游戏标题 -->
<TextView android:id="@+id/tvGameTitle" />

<!-- 自定义棋盘 -->
<BoardView android:id="@+id/boardView" />

<!-- 功能按钮 -->
<Button android:id="@+id/btnBack" />      <!-- 返回 -->
<Button android:id="@+id/btnDiscover" />  <!-- 扫描设备 -->
<Button android:id="@+id/btnHost" />      <!-- 等待连接 -->
<Button android:id="@+id/btnAI" />        <!-- AI对战 -->
<Button android:id="@+id/btnUndo" />      <!-- 悔棋 -->

<!-- 设备列表 -->
<MaterialCardView android:id="@+id/cardDevices">
    <ListView android:id="@+id/listDevices" />
</MaterialCardView>
```

**何时使用**: GameActivity.setContentView()加载

---

### 3. activity_history.xml
**路径**: `app/src/main/res/layout/activity_history.xml`

**作用**: HistoryActivity的布局文件
- 历史记录界面UI定义
- 记录列表显示

**关键组件**:
```xml
<!-- 标题 -->
<TextView android:text="对战历史" />

<!-- 历史记录列表 -->
<ListView android:id="@+id/listHistory" />

<!-- 返回按钮 -->
<Button android:id="@+id/btnBack" />
```

**何时使用**: HistoryActivity.setContentView()加载

---

### 4. item_history.xml
**路径**: `app/src/main/res/layout/item_history.xml`

**作用**: 历史记录列表项布局
- 定义单条历史记录的显示样式
- 显示游戏类型、对手、结果等信息

**关键组件**:
```xml
<!-- 游戏类型 -->
<TextView android:id="@+id/tvGameType" />

<!-- 对战模式和对手 -->
<TextView android:id="@+id/tvOpponent" />

<!-- 结果 -->
<TextView android:id="@+id/tvResult" />

<!-- 时长和步数 -->
<TextView android:id="@+id/tvDetails" />

<!-- 时间戳 -->
<TextView android:id="@+id/tvTimestamp" />
```

**何时使用**: ListView的Adapter中使用，为每条记录创建视图

---

## 📚 答辩文档（7个）

### 1. 答辩文档.md
**作用**: 完整的技术答辩文档
**内容**:
- 项目概述（名称、平台、代码量）
- 功能特点（三种游戏、AI、蓝牙、历史记录）
- 技术架构（应用结构、核心类设计）
- 技术亮点（面向对象、算法、通信协议）
- 项目统计（代码规模、类分布）
- 开发工具与技术
- 界面设计
- 创新点与优势
- 测试情况
- 学习收获

**何时使用**: 答辩准备、技术讲解

---

### 2. 答辩演示脚本.md
**作用**: 答辩演示的详细脚本
**内容**:
- 开场白（30秒）
- 项目背景（1分钟）
- 功能演示（3-4分钟）
  * Demo 1: 主菜单
  * Demo 2: AI对战
  * Demo 3: 象棋游戏
  * Demo 4: 蓝牙联机
  * Demo 5: 游戏历史
- 技术实现（2-3分钟）
- 创新点与亮点（1-2分钟）
- 总结陈述（30秒）
- 常见问题快速回答
- 时间分配建议
- 演示准备清单

**何时使用**: 答辩演示时作为流程指导

---

### 3. 技术问答.md
**作用**: 27个常见技术问题及详细回答
**内容分类**:
- Android开发相关（Activity生命周期、内存泄漏、自定义View）
- 算法与数据结构（AI算法、围棋提子、象棋规则）
- 蓝牙通信（协议设计、数据同步、权限申请）
- 数据存储（SharedPreferences选择、数据结构）
- UI/UX设计（Material Design、屏幕适配）
- 工程实践（代码组织、质量保证、挑战）
- 扩展与优化（在线对战、性能优化）
- 测试相关（单元测试、功能测试）
- 项目反思（改进方向、学习收获）

**何时使用**: 答辩前复习、临场应答参考

---

### 4. 答辩速查卡.md
**作用**: 快速参考卡（可打印）
**内容**:
- 核心数据（代码行数、类数量、功能数量）
- 功能清单
- 技术关键词
- 三句话介绍项目
- 必答题标准答案
- 加分回答模板
- 避坑指南
- 开场白和结束语（背诵版）
- Demo流程速记
- 紧急应对
- 检查清单

**何时使用**: 答辩当天随身携带、快速查阅

---

### 5. 应用启动流程和流程图.md
**作用**: 详细的启动流程技术文档
**内容**:
- 应用启动入口（从点击图标到MenuActivity）
- 完整应用流程图
- 详细启动流程（Phase 1-3）
- 游戏启动流程
- GameActivity初始化流程
- AI对战流程
- 蓝牙对战流程
- 历史记录流程
- Activity生命周期流程
- 数据流向图
- 关键流程总结
- 技术要点

**何时使用**: 讲解应用架构、启动机制

---

### 6. 流程图可视化.md
**作用**: 可视化流程图（ASCII艺术）
**内容**:
- 简化版整体流程（适合PPT）
- 详细交互流程图
- AI对战完整流程
- 蓝牙对战流程
- 数据流转图
- 游戏引擎切换流程
- 游戏记录保存流程
- 完整用户旅程
- 性能优化点

**何时使用**: PPT制作、流程讲解

---

### 7. 按钮功能和消息传递.md
**作用**: 按钮交互和消息传递详解
**内容**:
- MenuActivity按钮（4个）
  * 五子棋卡片
  * 围棋卡片
  * 象棋卡片
  * 历史按钮
- GameActivity按钮（6个）
  * 返回
  * 扫描设备
  * 等待连接
  * AI对战
  * 悔棋
  * 设备列表项
- 棋盘点击/拖动事件
- 消息传递总结（Intent、蓝牙、内部）
- 完整交互流程示例

**何时使用**: 讲解交互机制、消息传递

---

## 🔗 文件关系图

```
应用启动
   ↓
MenuActivity ←→ activity_menu.xml
   │
   ├─→ GameActivity ←→ activity_game.xml
   │      ├─→ GomokuEngine
   │      ├─→ GoEngine
   │      ├─→ XiangqiEngine
   │      ├─→ SimpleAI
   │      ├─→ BluetoothHelper
   │      ├─→ BoardView
   │      └─→ GameHistoryManager → GameRecord
   │
   └─→ HistoryActivity ←→ activity_history.xml
          └─→ item_history.xml
          └─→ GameHistoryManager → GameRecord
```

---

## 📊 文件使用频率

### 高频使用
- **GameActivity.java** - 核心游戏逻辑，使用最频繁
- **BoardView.java** - 每次落子都需要刷新
- **引擎类** - 每次落子时调用
- **activity_game.xml** - 游戏界面，使用时间最长

### 中频使用
- **MenuActivity.java** - 启动和返回时使用
- **SimpleAI.java** - AI对战模式时使用
- **BluetoothHelper.java** - 蓝牙对战模式时使用

### 低频使用
- **HistoryActivity.java** - 查看历史时使用
- **GameHistoryManager.java** - 游戏结束和查看历史时使用
- **GameRecord.java** - 数据封装，被动使用

---

## 💡 学习建议

### 按学习顺序
1. **入门**: MenuActivity → activity_menu.xml
2. **核心**: GameActivity → 三个引擎类
3. **进阶**: BoardView（自定义View）
4. **高级**: SimpleAI（算法）、BluetoothHelper（网络）
5. **补充**: 历史记录相关类

### 按功能模块
1. **界面层**: MenuActivity, GameActivity, HistoryActivity + 布局文件
2. **逻辑层**: 三个引擎类 + SimpleAI
3. **视图层**: BoardView
4. **通信层**: BluetoothHelper
5. **数据层**: GameHistoryManager, GameRecord

---

**此文档提供了项目所有文件的完整用法和作用说明，适合快速了解项目结构！** 📚
