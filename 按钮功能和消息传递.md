# 🔘 按钮交互和消息传递详解

## 📱 MenuActivity（主菜单界面）按钮

### 1. 🎯 五子棋卡片（cardGomoku）

**位置**：主菜单第一个游戏卡片

**点击事件**：
```java
cardGomoku.setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        Intent intent = new Intent(MenuActivity.this, GameActivity.class);
        intent.putExtra("GAME_TYPE", "GOMOKU");
        startActivity(intent);
    }
});
```

**功能说明**：
1. 创建Intent对象，指向GameActivity
2. 通过`putExtra()`传递消息：`"GAME_TYPE" = "GOMOKU"`
3. 调用`startActivity()`启动GameActivity

**消息传递过程**：
```
用户点击五子棋卡片
    ↓
MenuActivity.onClick()
    ↓
创建 Intent
    ↓
intent.putExtra("GAME_TYPE", "GOMOKU")  ← 传递游戏类型
    ↓
startActivity(intent)
    ↓
系统启动 GameActivity
    ↓
GameActivity.onCreate()
    ↓
String gameType = getIntent().getStringExtra("GAME_TYPE")  ← 接收消息
    ↓
初始化五子棋引擎（GomokuEngine）
```

**数据流向**：
- **发送方**：MenuActivity
- **消息类型**：String
- **键名**：`"GAME_TYPE"`
- **值**：`"GOMOKU"`
- **接收方**：GameActivity
- **传递机制**：Intent.putExtra() / getIntent().getStringExtra()

---

### 2. ⚫ 围棋卡片（cardGo）

**位置**：主菜单第二个游戏卡片

**点击事件**：
```java
cardGo.setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        Intent intent = new Intent(MenuActivity.this, GameActivity.class);
        intent.putExtra("GAME_TYPE", "GO");
        startActivity(intent);
    }
});
```

**功能说明**：
1. 创建Intent对象，指向GameActivity
2. 传递消息：`"GAME_TYPE" = "GO"`
3. 启动GameActivity

**消息内容**：
- 键：`"GAME_TYPE"`
- 值：`"GO"`（字符串）
- 作用：告诉GameActivity要初始化围棋游戏

---

### 3. ♟️ 象棋卡片（cardXiangqi）

**位置**：主菜单第三个游戏卡片

**点击事件**：
```java
cardXiangqi.setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        Intent intent = new Intent(MenuActivity.this, GameActivity.class);
        intent.putExtra("GAME_TYPE", "XIANGQI");
        startActivity(intent);
    }
});
```

**功能说明**：
1. 创建Intent对象
2. 传递消息：`"GAME_TYPE" = "XIANGQI"`
3. 启动GameActivity，初始化象棋游戏

**消息内容**：
- 键：`"GAME_TYPE"`
- 值：`"XIANGQI"`

---

### 4. 📜 对战历史按钮（btnHistory）

**位置**：主菜单底部按钮

**点击事件**：
```java
btnHistory.setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        Intent intent = new Intent(MenuActivity.this, HistoryActivity.class);
        startActivity(intent);
    }
});
```

**功能说明**：
1. 创建Intent对象，指向HistoryActivity
2. 不需要传递额外数据
3. 启动HistoryActivity显示历史记录

**消息传递**：
- 不传递额外消息
- 仅启动新Activity

---

## 🎮 GameActivity（游戏界面）按钮

### 5. ⬅️ 返回按钮（btnBack）

**位置**：游戏界面左上角

**点击事件**：
```java
findViewById(R.id.btnBack).setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        finish();
    }
});
```

**功能说明**：
1. 调用`finish()`方法
2. 结束当前GameActivity
3. 返回到MenuActivity

**消息传递**：
- 无需传递消息
- 通过Activity生命周期回退

**流程**：
```
用户点击返回
    ↓
finish()
    ↓
GameActivity.onPause()
    ↓
GameActivity.onStop()
    ↓
GameActivity.onDestroy()
    ↓
返回 MenuActivity
    ↓
MenuActivity.onRestart()
    ↓
MenuActivity.onResume()
```

---

### 6. 🔍 扫描设备按钮（btnDiscover）

**位置**：游戏界面蓝牙功能区

**点击事件**：
```java
findViewById(R.id.btnDiscover).setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        checkPermAndDiscover();
        cardDevices.setVisibility(View.VISIBLE);
    }
});
```

**功能说明**：
1. 调用`checkPermAndDiscover()`检查蓝牙权限并开始扫描
2. 显示设备列表卡片

**内部流程**：
```java
private void checkPermAndDiscover() {
    // 检查权限
    List<String> need = new ArrayList<>();
    for (String p : perms) {
        if (checkSelfPermission(p) != PackageManager.PERMISSION_GRANTED) {
            need.add(p);
        }
    }
    
    if (!need.isEmpty()) {
        // 权限不足，请求权限
        permLauncher.launch(need.toArray(new String[0]));
    } else {
        // 权限已授予，开始扫描
        startDiscovery();
    }
}

private void startDiscovery() {
    deviceAdapter.clear();
    deviceMap.clear();
    bt.startDiscovery();  // ← 调用蓝牙辅助类开始扫描
    Toast.makeText(this, "开始扫描...", Toast.LENGTH_SHORT).show();
}
```

**消息传递**：
```
点击扫描按钮
    ↓
checkPermAndDiscover()
    ↓
【如果需要权限】
    permLauncher.launch(permissions)  ← 向系统请求权限
        ↓
    用户授权
        ↓
    onActivityResult() 回调
        ↓
    startDiscovery()
    
【如果已有权限】
    startDiscovery()
        ↓
    bt.startDiscovery()  ← 调用BluetoothHelper
        ↓
    BluetoothHelper 开始蓝牙扫描
        ↓
    发现设备时回调 onDeviceFound()
        ↓
    更新设备列表 UI
```

**涉及的消息**：
- 权限请求消息（发送给系统）
- 蓝牙扫描指令（发送给BluetoothHelper）
- 设备发现事件（BluetoothHelper回调）

---

### 7. 📡 等待连接按钮（btnHost）

**位置**：游戏界面蓝牙功能区

**点击事件**：
```java
findViewById(R.id.btnHost).setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        bt.startServerAccept();
    }
});
```

**功能说明**：
1. 调用BluetoothHelper的`startServerAccept()`
2. 开启蓝牙服务端，等待其他设备连接
3. 作为主机等待客户端连接

**消息传递流程**：
```
点击等待连接
    ↓
bt.startServerAccept()
    ↓
BluetoothHelper 创建服务端Socket
    ↓
监听蓝牙连接
    ↓
【当有设备连接时】
    onConnected(BluetoothDevice) 回调
    ↓
    显示 Toast："已连接 XXX"
    ↓
    sendChallenge()  ← 发送对战邀请消息
```

**发送的消息**：
```json
{
  "type": "challenge",
  "from": "Player",
  "gameType": "GOMOKU"
}
```

---

### 8. 🤖 AI对战按钮（btnAI）

**位置**：游戏界面对战模式选择区

**点击事件**：
```java
findViewById(R.id.btnAI).setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        showAIDifficultyDialog();
    }
});
```

**功能说明**：
1. 显示AI难度选择对话框
2. 用户选择难度后开始AI对战

**内部流程**：
```java
private void showAIDifficultyDialog() {
    String[] difficulties = {"简单", "中等", "困难"};
    new AlertDialog.Builder(this)
        .setTitle("选择AI难度")
        .setItems(difficulties, new DialogInterface.OnClickListener() {
            @Override
            public void onClick(DialogInterface dialog, int which) {
                SimpleAI.Difficulty difficulty;
                switch (which) {
                    case 0: difficulty = SimpleAI.Difficulty.EASY; break;
                    case 1: difficulty = SimpleAI.Difficulty.MEDIUM; break;
                    case 2: difficulty = SimpleAI.Difficulty.HARD; break;
                    default: difficulty = SimpleAI.Difficulty.MEDIUM;
                }
                startAIGame(difficulty);  // ← 启动AI游戏
            }
        })
        .setNegativeButton("取消", null)
        .show();
}

private void startAIGame(SimpleAI.Difficulty difficulty) {
    // 创建AI实例
    ai = new SimpleAI(difficulty);
    playingAgainstAI = true;
    gameStarted = true;
    
    // 重置游戏引擎
    // 根据游戏类型初始化相应引擎
}
```

**消息传递（内部）**：
```
点击AI对战
    ↓
显示对话框
    ↓
用户选择难度 (0/1/2)  ← 对话框内部消息
    ↓
startAIGame(difficulty)
    ↓
设置游戏状态：
    - playingAgainstAI = true
    - gameStarted = true
    - 创建 SimpleAI 实例
    ↓
Toast 提示："AI对战开始！"
```

**不涉及网络消息**：
- 完全本地操作
- 不需要蓝牙或网络通信

---

### 9. ↩️ 悔棋按钮（btnUndo）

**位置**：游戏界面操作区

**点击事件**：
```java
findViewById(R.id.btnUndo).setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        if (playingAgainstAI) {
            performUndo();  // AI对战：直接悔棋
        } else {
            sendUndoRequest();  // 蓝牙对战：发送悔棋请求
        }
    }
});
```

**功能说明**：
- 根据当前游戏模式采取不同操作
- AI对战：直接撤销两步（玩家+AI）
- 蓝牙对战：发送悔棋请求给对手

#### 9.1 AI对战悔棋

```java
private void performUndo() {
    if (currentGameType == BoardView.GameType.GOMOKU) {
        gomokuEngine.undo(2);  // 撤销2步
    } else if (currentGameType == BoardView.GameType.GO) {
        goEngine.undo(2);
    } else if (currentGameType == BoardView.GameType.XIANGQI) {
        xiangqiEngine.undo(2);
    }
    boardView.invalidate();  // 刷新棋盘
    turn = myColor;  // 恢复玩家回合
}
```

**消息传递**：
```
点击悔棋（AI模式）
    ↓
performUndo()
    ↓
调用引擎 undo(2)  ← 内部方法调用
    ↓
刷新 BoardView
```

#### 9.2 蓝牙对战悔棋

```java
private void sendUndoRequest() {
    try {
        JSONObject o = new JSONObject();
        o.put("type", "undo_request");
        sendMessage(o.toString());
    } catch (Exception e) {
        Toast.makeText(this, "发送悔棋请求失败", Toast.LENGTH_SHORT).show();
    }
}
```

**消息传递流程**：
```
点击悔棋（蓝牙模式）
    ↓
sendUndoRequest()
    ↓
构造JSON消息：{"type": "undo_request"}
    ↓
sendMessage(json)  ← 发送给BluetoothHelper
    ↓
BluetoothHelper.sendLine(json)
    ↓
通过蓝牙Socket发送给对方设备
    ↓
【对方设备】
    接收消息
    ↓
    onMessage() 回调
    ↓
    显示对话框："同意撤销上一步吗？"
    ↓
    【如果同意】
        执行 undo(1)
        ↓
        发送回复：{"type": "undo_ok"}
    ↓
【本设备接收回复】
    onMessage() 接收 "undo_ok"
    ↓
    执行 undo(1)
    ↓
    刷新棋盘
```

**发送的消息**：
```json
// 悔棋请求
{
  "type": "undo_request"
}

// 同意悔棋（对方发送）
{
  "type": "undo_ok"
}
```

---

### 10. 🔗 设备列表项点击

**位置**：蓝牙设备列表

**点击事件**：
```java
list.setOnItemClickListener(new AdapterView.OnItemClickListener() {
    @Override
    public void onItemClick(AdapterView<?> p, View v, int pos, long id) {
        String key = deviceAdapter.getItem(pos);
        BluetoothDevice d = deviceMap.get(key);
        if (d != null) {
            bt.connectTo(d);  // ← 连接到选中的设备
        }
    }
});
```

**功能说明**：
1. 用户从扫描到的设备列表中选择一个设备
2. 获取对应的BluetoothDevice对象
3. 调用`bt.connectTo()`建立连接

**消息传递流程**：
```
用户点击设备列表项
    ↓
获取设备信息 (BluetoothDevice)
    ↓
bt.connectTo(device)
    ↓
BluetoothHelper 建立连接
    ↓
【连接成功】
    onConnected(device) 回调
    ↓
    显示 Toast："已连接 XXX"
    ↓
    sendChallenge()  ← 发送对战邀请
        ↓
        构造消息：{"type": "challenge", "from": "Player"}
        ↓
        通过蓝牙发送给对方
        ↓
【对方设备】
    接收 challenge 消息
    ↓
    显示对话框："是否接受对战？"
    ↓
    【用户点击接受】
        发送：{"type": "accept"}
        ↓
        gameStarted = true
        myColor = 2 (后手)
    ↓
【本设备接收 accept】
    Toast："对方接受，开始游戏"
    ↓
    gameStarted = true
    myColor = 1 (先手)
```

**发送的消息**：
```json
// 对战邀请
{
  "type": "challenge",
  "from": "Player",
  "gameType": "GOMOKU"
}

// 接受对战
{
  "type": "accept"
}

// 拒绝对战
{
  "type": "reject"
}
```

---

## 🎲 棋盘点击/拖动事件

### 11. 棋盘落子（五子棋/围棋）

**事件设置**：
```java
boardView.setOnPlaceListener(new BoardView.OnPlaceListener() {
    @Override
    public void onPlace(int r, int c) {
        tryPlace(r, c);  // ← 尝试在(r,c)位置落子
    }
});
```

**功能说明**：
1. 用户点击棋盘某个位置
2. BoardView计算点击的行列坐标(r, c)
3. 回调`tryPlace(r, c)`

**内部流程**：
```java
private void tryPlace(int r, int c) {
    if (!gameStarted) {
        Toast.makeText(this, "请先开始游戏", Toast.LENGTH_SHORT).show();
        return;
    }
    if (turn != myColor) {
        Toast.makeText(this, "等待对手", Toast.LENGTH_SHORT).show();
        return;
    }
    
    boolean success = false;
    if (currentGameType == BoardView.GameType.GOMOKU) {
        success = gomokuEngine.place(r, c, myColor);
    } else if (currentGameType == BoardView.GameType.GO) {
        success = goEngine.place(r, c, myColor);
    }
    
    if (!success) return;
    
    playerMoveCount++;
    boardView.invalidate();  // 刷新棋盘
    
    if (!playingAgainstAI) {
        sendMove(r, c, myColor, -1, -1);  // ← 蓝牙对战：发送移动消息
    }
    
    // 检查胜负
    boolean won = false;
    if (currentGameType == BoardView.GameType.GOMOKU) {
        won = gomokuEngine.checkWin(r, c);
    } else if (currentGameType == BoardView.GameType.GO) {
        won = goEngine.checkWin(r, c);
    }
    
    if (won) {
        gameEnded = true;
        showGameEndDialog("你赢了!");
    } else {
        turn = other(myColor);
        if (playingAgainstAI) {
            makeAIMove();  // ← AI对战：触发AI落子
        }
    }
}
```

**蓝牙对战消息**：
```java
private void sendMove(int r, int c, int color, int fromR, int fromC) {
    try {
        JSONObject o = new JSONObject();
        o.put("type", "move");
        o.put("r", r);
        o.put("c", c);
        o.put("color", color);
        o.put("gameType", currentGameType.name());
        sendMessage(o.toString());
    } catch (Exception e) {
        Toast.makeText(this, "发送移动失败", Toast.LENGTH_SHORT).show();
    }
}
```

**发送的消息**：
```json
{
  "type": "move",
  "r": 7,
  "c": 8,
  "color": 1,
  "gameType": "GOMOKU"
}
```

**消息传递流程**：
```
用户点击棋盘(r, c)
    ↓
tryPlace(r, c)
    ↓
引擎.place(r, c, color)  ← 内部方法调用
    ↓
刷新 BoardView
    ↓
【如果是蓝牙对战】
    sendMove(r, c, color)
    ↓
    构造JSON消息
    ↓
    通过蓝牙发送给对方
    ↓
【对方设备】
    接收 move 消息
    ↓
    onRemoteMove(r, c, color)
    ↓
    更新棋盘
    
【如果是AI对战】
    makeAIMove()
    ↓
    AI计算最优位置
    ↓
    AI落子
    ↓
    刷新棋盘
```

---

### 12. 象棋移动

**事件设置**：
```java
boardView.setOnXiangqiMoveListener(new BoardView.OnXiangqiMoveListener() {
    @Override
    public void onMove(int fromR, int fromC, int toR, int toC) {
        tryXiangqiMove(fromR, fromC, toR, toC);
    }
});
```

**功能说明**：
1. 用户拖动棋子从(fromR, fromC)到(toR, toC)
2. 调用`tryXiangqiMove()`验证并执行移动

**发送的消息**（蓝牙对战）：
```json
{
  "type": "move",
  "r": 5,
  "c": 4,
  "color": 1,
  "gameType": "XIANGQI",
  "fromR": 4,
  "fromC": 4
}
```

---

## 📊 消息传递总结

### Intent消息（Activity间）

| 发送方 | 接收方 | 消息键 | 消息值 | 作用 |
|--------|--------|--------|--------|------|
| MenuActivity | GameActivity | "GAME_TYPE" | "GOMOKU" | 启动五子棋游戏 |
| MenuActivity | GameActivity | "GAME_TYPE" | "GO" | 启动围棋游戏 |
| MenuActivity | GameActivity | "GAME_TYPE" | "XIANGQI" | 启动象棋游戏 |
| MenuActivity | HistoryActivity | (无) | (无) | 显示历史记录 |

### 蓝牙消息（JSON格式）

| 消息类型 | JSON结构 | 发送时机 | 作用 |
|----------|----------|----------|------|
| challenge | `{"type":"challenge","from":"Player"}` | 连接成功后 | 邀请对战 |
| accept | `{"type":"accept"}` | 接受邀请 | 同意对战 |
| reject | `{"type":"reject"}` | 拒绝邀请 | 拒绝对战 |
| move | `{"type":"move","r":7,"c":8,"color":1}` | 每次落子 | 同步棋局 |
| undo_request | `{"type":"undo_request"}` | 点击悔棋 | 请求悔棋 |
| undo_ok | `{"type":"undo_ok"}` | 同意悔棋 | 确认悔棋 |

### 内部消息（方法调用）

| 调用方 | 被调用方法 | 参数 | 作用 |
|--------|------------|------|------|
| GameActivity | GomokuEngine.place() | r, c, color | 五子棋落子 |
| GameActivity | GoEngine.place() | r, c, color | 围棋落子 |
| GameActivity | XiangqiEngine.place() | fromR, fromC, toR, toC, color | 象棋移动 |
| GameActivity | SimpleAI.findBestMove() | board, color, size | AI计算最优位置 |
| GameActivity | BluetoothHelper.startDiscovery() | (无) | 开始蓝牙扫描 |
| GameActivity | BluetoothHelper.connectTo() | BluetoothDevice | 连接设备 |
| GameActivity | BluetoothHelper.sendLine() | String | 发送蓝牙消息 |

---

## 🔄 完整交互流程示例

### 示例1：启动五子棋游戏

```
1. 用户点击 "五子棋" 卡片
   ↓
2. MenuActivity.onClick()
   - 创建 Intent
   - putExtra("GAME_TYPE", "GOMOKU")  ← 消息1
   - startActivity(intent)
   ↓
3. GameActivity.onCreate()
   - getStringExtra("GAME_TYPE")  ← 接收消息1
   - 判断为 "GOMOKU"
   - 创建 GomokuEngine(15)
   - boardView.bindGomokuEngine(engine)
   - 显示五子棋界面
```

### 示例2：蓝牙对战完整流程

```
【设备A】
1. 点击 "等待连接" 按钮
   ↓
2. bt.startServerAccept()
   - 开启蓝牙服务端
   - 等待连接...

【设备B】
3. 点击 "扫描设备" 按钮
   ↓
4. checkPermAndDiscover()
   - 检查权限
   - bt.startDiscovery()
   - 发现设备A
   ↓
5. 点击设备A
   ↓
6. bt.connectTo(deviceA)
   - 建立连接
   ↓
7. onConnected() 回调
   ↓
8. sendChallenge()
   - 发送: {"type":"challenge"}  ← 蓝牙消息1

【设备A】
9. onMessage() 接收
   - 解析: type = "challenge"
   - 显示对话框
   ↓
10. 用户点击 "接受"
    ↓
11. 发送: {"type":"accept"}  ← 蓝牙消息2
    - myColor = 2
    - gameStarted = true

【设备B】
12. onMessage() 接收
    - 解析: type = "accept"
    - Toast: "对方接受，开始游戏"
    - myColor = 1
    - gameStarted = true
    ↓
13. 游戏开始，轮流下棋...
```

### 示例3：AI对战流程

```
1. 点击 "AI对战" 按钮
   ↓
2. showAIDifficultyDialog()
   - 显示对话框
   ↓
3. 用户选择 "中等"
   ↓
4. onClick(which = 1)  ← 对话框内部消息
   - difficulty = MEDIUM
   - startAIGame(MEDIUM)
   ↓
5. 创建 SimpleAI(MEDIUM)
   - playingAgainstAI = true
   - gameStarted = true
   ↓
6. 用户点击棋盘(7, 8)
   ↓
7. tryPlace(7, 8)
   - gomokuEngine.place(7, 8, 1)
   - 检查胜负
   - turn = 2
   - makeAIMove()  ← 触发AI
   ↓
8. AI计算（后台线程）
   - ai.findBestMove()  ← 内部方法调用
   - 返回最优位置 (6, 7)
   ↓
9. AI落子(6, 7)
   - gomokuEngine.place(6, 7, 2)
   - 刷新棋盘
   - turn = 1
   ↓
10. 继续游戏...
```

---

**此文档详细说明了所有按钮的功能和消息传递机制，适合答辩时讲解交互流程！** 🎯
